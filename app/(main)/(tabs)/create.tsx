/**
 * Create Tab Placeholder
 * 
 * This screen is never shown - the tab opens a modal instead.
 */

import React from 'react';
import { View } from 'react-native';

export default function CreateScreen() {
    return <View />;
}
